base = float(input("Введите основание треугольника: "))
height = float(input("Введите высоту треугольника: "))

# Расчет площади по формуле S = 0.5 * a * h
area = 0.5 * base * height

print(f"Площадь треугольника: {area}")
